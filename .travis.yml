language: ruby

rvm:
  - 2.7.1

services:
  - postgresql

install:
  - bundle install

before_script:
  - bundle exec rake db:create
  - bundle exec rake db:migrate

script:
  - bundle exec rspec
 
notification:
  - email: false
